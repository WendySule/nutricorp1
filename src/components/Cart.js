import React from 'react'
import CartMenu from './CartMenu'
import CartContainer from './CartContainer'

function Cart() {
  return (
    <div>
        <CartMenu/>
        <CartContainer/>
    </div>
  );
}

export default Cart;